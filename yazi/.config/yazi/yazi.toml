[manager]
show_hidden = true
linemode = "size"

# [opener]
# play = [
# 	{ run = 'mpv "$@"', orphan = true, for = "unix" },
# ]
# edit = [
# 	{ run = '$EDITOR "$@"', block = true, for = "unix" },
# ]
# open = [
# 	{ run = 'xdg-open "$@"', desc = "Open" },
# ]
